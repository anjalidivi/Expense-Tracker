# Expense-Tracker
Expense Tracker is a responsive web app using HTML, CSS, and JavaScript that helps users manage finances by setting a budget, adding categorized expenses, viewing total spent and remaining balance, and deleting expenses. Data is saved locally via LocalStorage for persistence without a backend.
